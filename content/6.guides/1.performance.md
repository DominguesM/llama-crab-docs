---
title: Performance
description: Tune llama-crab performance by adjusting build mode, model parameters, threads, GPU offload, and measurement strategy.
navigation:
  icon: i-lucide-gauge
seo:
  title: Performance
  description: Tune llama-crab performance by adjusting build mode, model parameters, threads, GPU offload, and measurement strategy.
---

These guides cover the operational choices that usually matter after the
first example runs.

Start with the smallest model that exercises your workload, then tune one
variable at a time.

```bash [Terminal]
llama-crab-server --model /models/small-chat.gguf
```

## Build in release mode

Use `--release` for realistic inference behavior:

```bash [Terminal]
cargo run --release
```

Debug builds are useful for Rust diagnostics, but llama.cpp inference is
much slower and some backend-sensitive paths are easier to evaluate in
release mode.

## Context and batch size

`n_ctx` controls the maximum prompt plus generation window. Larger values
use more memory. `n_batch` and `n_ubatch` control how much work is fed
through the model at once.

```rust
let params = LlamaParams::new("models/model.gguf")
    .with_n_ctx(4096)
    .with_n_batch(512)
    .with_n_ubatch(256);
```

If allocation fails, reduce context first, then batch sizes, then choose a
smaller quantized model.

## CPU threads

Set explicit thread counts when you want reproducible local behavior:

```rust
let params = LlamaParams::new("models/model.gguf")
    .with_n_threads(4)
    .with_n_threads_batch(4);
```

The server exposes the same knobs as `--n-threads` and `--n-batch` style
flags (see [Server: Running](/server/running) for the full flag table).

## GPU offload

`with_n_gpu_layers` controls how many layers are offloaded to the active
backend:

```rust
let params = LlamaParams::new("models/model.gguf").with_n_gpu_layers(99);
```

The right value depends on model size, backend, and available VRAM or
unified memory. If startup fails after increasing offload, lower this
value before changing unrelated settings. `-1` (or `99`) means "offload
every layer"; pass an explicit number when you want to leave a few layers
on the CPU for host memory pressure.

## Feature selection

The default crate features enable `openmp` and `metal`. On non-Apple
targets or CI, you may want explicit features:

```toml
llama-crab = { version = "0.1.6", default-features = false, features = ["openmp"] }
```

Use only one primary accelerator backend unless you have a reason to
compile several (e.g. `metal` + `vulkan` is usually wasted, but
`vulkan` + `opencl` can be useful for cross-vendor CI).

## Measure the right layer

For application tuning, measure:

- model load time,
- prompt token count,
- time to first token,
- generated tokens per second,
- peak memory.

For serious benchmarking, use a dedicated harness with fixed prompts,
fixed sampler settings, and warm caches. Repository examples and wrappers
for local validation are covered in
[Development](/contributing/development).

## Mobile presets

For phones, tablets, and other memory-constrained targets, prefer the
built-in [Mobile presets](/guides/mobile) (`MobilePreset::LowRam`,
`Balanced`, `GpuMax`) before hand-tuning every parameter. The server
mirrors them through `--mobile-preset low-ram | balanced | gpu-max`.
